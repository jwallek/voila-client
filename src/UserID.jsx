import React, {useState} from 'react'
import App from './App'

function UserId({loggedInID}) {
    const[userLoggedInID, setUserLoggedInID] = useState('')
    setUserLoggedInID(loggedInID)

  return (
    0
  )
}

export default  UserId